services:
  whoami:
    image: traefik/whoami
    restart: unless-stopped
    networks: [traefik-public]
    labels:
      traefik.enable: "true"
      traefik.docker.network: traefik-public
      traefik.http.routers.whoami.rule: Host(`whoami-hosting.theochinomona.tech`)
      traefik.http.routers.whoami.entrypoints: web

networks:
  traefik-public:
    external: true

# Phase 6 push test marker: 1
# Phase 6 push test marker: 2
# Phase 6 push test marker: 3
# Phase 6 push test marker: 4
# Phase 6 push test marker: 5
# Phase 6 push test marker: 6
# Phase 6 push test marker: 7
# Phase 6 push test marker: 8
# Phase 6 push test marker: 9
# Phase 6 push test marker: 10
# Phase 6 push test marker: 11
